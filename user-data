## template: jinja
#cloud-config

{%- from '/etc/cloud/cloud.cfg' import system_info with context %}

runcmd:
  - myvar=`echo 'This is my var'`
  - echo $myvar > /root/myvar
  - echo {{ ds.meta_data.hostname }} > /root/hostname
  - echo {{ system_info }} > /root/testing
  
