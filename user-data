## template: jinja
#cloud-config

runcmd:
  - myvar=`echo 'This is my var'`
  - echo $myvar > /root/myvar
  - echo {{ ds.meta_data.hostname }} > /root/hostname
  - echo {{ system_info }} > /root/sytem
  - echo {{ system_info_distro }} > /root/sytem
  
